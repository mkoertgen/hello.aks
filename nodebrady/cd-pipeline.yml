trigger: none
pr: none

resources: 
  pipelines:
  - pipeline: ci-pipeline
    source: nodebrady-ci
    trigger:
      enabled: true
      branches:
        include:
          - master

stages:
- template: ../common/templates/cd-stages-template.yml
  parameters:
    component: nodebrady
