trigger: none
pr: none

resources: 
  pipelines:
  - pipeline: ci-pipeline
    source: parrot-ci
    trigger:
      enabled: true
      branches:
        include:
          - master

stages:
- template: ../common/cd-stages-template.yml
  parameters:
    component: parrot
